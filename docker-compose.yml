version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: kahoot_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./database/db_init.sh:/docker-entrypoint-initdb.d/1_init.sh
      - ./database/seed.sql:/docker-entrypoint-initdb.d/2_seed.sql

  phpmyadmin:
      image: phpmyadmin/phpmyadmin
      container_name: kahoot_phpmyadmin
      restart: always
      ports:
        - "8080:80"
      environment:
        PMA_HOST: db
        PMA_PORT: 3306
      depends_on:
        - db

volumes:
  db_data: